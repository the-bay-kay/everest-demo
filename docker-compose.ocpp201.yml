version: "3.6"

services:
  mqtt-server:
    image: ghcr.io/everest/everest-demo/mqtt-server:${TAG}
    platform: linux/x86_64
    logging:
      driver: none

  manager:
    # image: ghcr.io/everest/everest-demo/manager:${TAG}
    build: ./manager
    platform: linux/x86_64
    deploy:
      resources:
        limits:
          cpus: "4" #"${EVEREST_MANAGER_CPUS}"
          memory: "8GB" #"${EVEREST_MANAGER_MEMORY}"
    depends_on:
      - mqtt-server
    environment:
      - MQTT_SERVER_ADDRESS=mqtt-server
    entrypoint: "tail -f /dev/null"
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=0
    extra_hosts:
      - "host.docker.internal:host-gateway"

  nodered:
    # image: ghcr.io/everest/everest-demo/nodered:${TAG}
    build: ./nodered
    depends_on:
      - mqtt-server
    ports:
      - 1880:1880
    environment:
      - MQTT_SERVER_ADDRESS=mqtt-server
      - FLOWS=/config/config-sil-two-evse-flow.json
